import { scale, verticalScale, moderateScale } from 'react-native-size-matters';

/**
 * scale = setting width
 * verticalScale = setting height
 * moderateScale = setting padding, margin, font-size etc...
 */

export { scale, verticalScale, moderateScale };